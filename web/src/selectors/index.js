
// app
export const appAppIdsSelector = (state) => state.app.appIds
export const appConfigsSelector = (state) => state.app.configs
export const appCommitsSelector = (state) => state.app.commits
export const appInstancesSelector = (state) => state.app.instances

// kube
export const kubeDeploymentsSelector = (state) => state.kube.deployments
